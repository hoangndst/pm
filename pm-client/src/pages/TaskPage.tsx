import * as React from 'react';
import { MainWrapper } from '../components/PageComponents';
import { SideBar } from '../layouts/SideBar';

export const TaskPage = () => {
  const [isMobileMenuOpen, setIsMobileMenuOpen] = React.useState(false);

  return (
    <>
      {/* <MainWrapper>
        
      </MainWrapper> */}
    </>
  )
}
